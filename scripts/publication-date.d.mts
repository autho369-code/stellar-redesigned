// Hand-written types for the plain-JS helper shared by the build scripts and
// vite.config.ts. The script stays .mjs because the Node build steps run it
// directly, without a TypeScript step.
export declare function getChicagoPublicationDate(date?: Date): string;
